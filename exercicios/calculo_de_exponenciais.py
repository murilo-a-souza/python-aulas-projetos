# Código do Algoritmo de Cálculo de Exponenciais

numBase = float(input('Número base: '))
numExpoente = float(input('Número expoente: '))
resultado = numBase ** numExpoente
print('O resultado da exponenciação de ', numBase, " e ", numExpoente, " é ", resultado)